# Python
.venv/
__pycache__/
*.pyc

# OS
.DS_Store

# Large data files
archive/processed/trades.csv
archive/goldsky/orderFilled.csv
archive.tar.xz
archive/processed/underdog_events/fills_sorted.parquet

# Reports / outputs (keep if you want; remove if you want tracked)
# reports/* (not reports/) so the prereg/eval negations below can re-include those dirs —
# git cannot re-include anything under a directory that is itself excluded.
reports/*
!reports/prereg/
!reports/eval/
data/
*.pem
