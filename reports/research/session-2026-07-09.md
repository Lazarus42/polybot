# Session log — 2026-07-09 (research-loop day 1)

_In-depth record so a later session can resume exactly where this one left off. Written at the end
of the session, before any forward P&L existed._

**Claude Code session id: `827b1b8f-6e1c-4bb8-93a2-b959ae2971c7`**
(project dir `-Users-spencergibson-Desktop-polymarket-exp`; transcripts/subagent outputs under
`~/.claude/projects/-Users-spencergibson-Desktop-polymarket-exp/` and
`/private/tmp/claude-501/-Users-spencergibson-Desktop-polymarket-exp/827b1b8f-.../`).
Persistent memory: `~/.claude/projects/-Users-spencergibson-Desktop-polymarket-exp/memory/`
(`research-loop-objective.md` holds the goal + operating constraints).

**Goal (user-set via /goal, still active):** run the research loop in
`FABLE_RESEARCH_AGENT_PROMPT.md` to find and out-of-sample-validate paper-trading strategies with
positive post-fill risk-adjusted return on a ≤$5k bankroll, strictly under the §4/§4a
pre-registration discipline in `RESEARCH_LOOP_CONTEXT.md`; never trade real money.

## What happened today, in order

1. **Housekeeping.** Context docs + analysis scripts were untracked → committed. Local exec-bit
   drift on `deploy/upload_to_s3.sh` fixed. `.gitignore` now tracks `reports/prereg/`,
   `reports/eval/`, `reports/research/`.
2. **Cycle 1 — capacity ceiling (analysis-only, pre-registered, CLOSED same day).**
   Prereg `reports/prereg/longshot-capacity-ceiling-2026-07-09.md`; eval
   `reports/eval/longshot-capacity-ceiling-2026-07-09.md`. Verdict **CONFIRMED
   capacity-constrained** (median concurrent in-band ask depth $4,839 < $5,000 — marginal, fully
   disclosed): median eligible market has ~$3 of in-band ask depth at entry; the sim's 1-clip
   footprint peaked at $3.3k; deployment is a ~$5.2k/day FLOW with ~2h median holds. Per-cycle ROC
   must never be quoted as bankroll return. New tool: `scripts/longshot_capacity_analysis.py`.
3. **User override:** deploy freeze lifted; up to 20 new strategies authorized; subagents +
   deep research requested. (Mid-window sim restarts for the in-flight 07-22 eval accepted and
   documented.)
4. **Research fan-out (all recorded in `reports/research/`):**
   - `external-evidence-2026-07-09.md` — 104-agent verified deep research. Tradable side of the
     favorite-longshot bias = BUYING 55–90¢ favorites (politics, long horizon); sub-10¢ longshot
     buying is a documented loser in 5–95¢ studies (our 4–5¢ cell is out of their sample); maker
     edge real but thin/high-variance; category-maker-alpha and spread-magnitude claims REFUTED;
     feed aggressor-side inference ~41% wrong (no flow-following).
   - `mining-insample-2026-07-09.md` — frozen-window mining (+ SAME-DAY ADDENDUM with corrected
     numbers, see 7). Maker taxonomy: {tech, politics, weather, world} net-positive for every core
     maker; sports/soccer/untagged bleed. tail_favorite: NO viable cell (thin-book effect
     inverted) → no tail variants. Longshot: sharp cliff at 4¢; horizon and pool gates promising.
5. **Expansion deployed (prereg `reports/prereg/paper-sim-expansion-2026-07-09.md`, 3 experiments,
   committed BEFORE deploy).** 17 new configs (16 + same-day `longshot_thin_0_3d` amendment) on
   top of the original 10 — 27 total, every family keeps its control:
   - A (longshot, eval **2026-07-30**): thin_shortdated(≤14d), thin_0_3d(<3d), thin_norewards
     (pool≤$1/day), thin_expolitics, thin_tp15/tp30, thin_2x/5x (capacity probes), 5_7_thin,
     thin_5k. Control: longshot_thin. Gate ≥500 resolved/arm; criteria in prereg.
   - B (favorites, eval **2026-08-09**): fav_hold (control), fav_politics, fav_far(≥7d),
     fav_politics_far. Gate ≥300 resolved/arm.
   - C (makers, eval **2026-07-30**): predict_skew_gencat, take_profit_3c_gencat, neutral_gencat
     (categories {tech,politics,weather,world}) vs ungated originals on net_if_flat.
   - **Window for ALL expansion arms: `--since 1783596212`** (2026-07-09 11:23:32 UTC, final
     redeploy). New sim capabilities shipped: Holder take_profit_price/stop_loss_price (sell at
     best bid), size_mult, per-config gates max/min_horizon_days (unknown passes), categories,
     exclude_categories, max_reward_pool; `load_token_meta` now carries category/horizon/neg_risk.
6. **Infrastructure fixes (user-directed "make those fixes now"):**
   - **Manifest coverage FIXED.** Root cause: analyses joined 1–2 point-in-time manifests (~12% of
     tokens each); S3 had 494. `scripts/build_token_tags.py` merges all → 7.6MB artifact,
     112,909 tokens, at `s3://polybot-polymarket-sjgibson/tags/token_tags.json.gz`; rebuilt daily
     on the box (`deploy/build_token_tags_cron.sh`, cron 04:41 UTC). Unknown tags on frozen
     window: 46.8%→0.0% (category/pool), 48.2%→3.1% (horizon).
   - **Arb monitor LIVE (observation-only).** `scripts/arb_monitor.py`, box cron `2-59/5 * * * *`:
     negRisk basket + YES/NO parity deviations with min-touch-depth $ bound, from Gamma + CLOB
     REST; ships to `s3://…/arb/{arb,scan}_YYYYMMDD.jsonl` (scan lines log zero-opportunity runs).
     First run: 741 baskets + 20.7k binaries discovered.
7. **Methodology corrections the coverage fix exposed (frozen window, honest and committed):**
   - **Entry-bucket smearing:** entries were bucketed on first post-fill snapshot mid; true fill
     recovered exactly as `mid − marked_pnl/inv` (total P&L reconciles unchanged at +$6,822).
     **Headline cell corrects: ≥4¢-fill ∧ <10k = +15.2% ROC / +$8,659 / n=13,306** (was
     +21.5% / +$14,194 / 9,153). True avg fill 6.1¢. Honest post-slippage planning: **~7–8%
     per-cycle ROC**. Pre-registered eval bars UNCHANGED (benchmark corrected, not the bar).
   - **Look-ahead horizon:** last-seen manifest horizon collapses to ~0 for anything watched to
     its end. New ex-ante `horizon_at_entry` axis (last_seen + horizon_days − e_t): edge is
     **<3d** (+39.2% in-cell), 3–14d negative → motivated the `longshot_thin_0_3d` amendment.
     Deployed live gates were always ex-ante correct.
   - **Pool de-confound:** no-reward-pool profitability SURVIVES full tags (+7.9% vs −8.9%),
     weaker than partial-tags estimate but real → `longshot_thin_norewards` stands.
   - See `LONGSHOT_STATE_OF_PLAY.md` §4b + the mining addendum.
8. **Tests:** 223 total; 33 added today; 1 known pre-existing unrelated failure
   (`test_impact_calibration.test_pandas_loader_end_to_end`, fails on clean tree).
9. **Commits:** 16 today, `8243cb4..b78c176`, all pushed to `Lazarus42/polybot` master.

## Live state (as of session end ~11:30 UTC)

- **EC2 184.72.90.164:** `polybot-paper-sim` (27 configs, pid window since epoch 1783596212) +
  `polybot-collect-all`, both active, 0 tracebacks. Box crons: uploader */15, token-tags 04:41 UTC,
  arb-monitor 2-59/5.
- **S3 prefixes:** `paper/` (fresh), `manifests/`, `tags/`, `arb/` (new today), `raw/` (never sync).
- **In-flight pre-registered evals (ALL P&L sealed until their dates):**
  1. `longshot-thin-evaluation-2026-07-22` (claude.ai scheduled task, window `--since 1783468800`,
     ≥1,000 resolved gate; two operator-authorized mid-window restarts today are documented in
     LONGSHOT_STATE_OF_PLAY §7 and must be mentioned in the eval).
  2. Expansion A+C → **2026-07-30**, B → **2026-08-09**, window `--since 1783596212`
     (`reports/prereg/paper-sim-expansion-2026-07-09.md` has gates + criteria; cloud routines for
     these could NOT be created — GitHub not connected + no AWS creds in cloud sandbox — so they
     must be fired by the user's scheduled-task mechanism or a live session on those dates).
- **This session's automation (dies with the session):** daily 10:07 local P&L-blind health-check
  cron (id 780c42ee, 7-day expiry, prompt says to re-create itself) + hourly ScheduleWakeup
  heartbeat.

## How to resume in a fresh session

1. Read `RESEARCH_LOOP_CONTEXT.md` (master index — updated today), then this file,
   `LONGSHOT_STATE_OF_PLAY.md`, the two prereg files, and `reports/research/*2026-07-09*`.
2. Re-set the goal (verbatim above) with /goal.
3. Until an eval date: DAILY P&L-BLIND health check only (service active? S3 paper/ fresh? 27
   configs in cmdline? crons intact? no tracebacks). NEVER read net_if_flat/marked_pnl
   aggregates/paper_sim_summary before an eval date.
4. On an eval date: sync `paper/` + `tags/token_tags.json.gz` (never `raw/`), run
   `scripts/longshot_market_analysis.py --config <arm> --since <window>` (holders) or
   `scripts/analyze_paper_sim.py --since <window>` (makers) for treatment AND control, apply the
   prereg gates/criteria EXACTLY as written, write `reports/eval/<id>.md`, update
   `RESEARCH_LOOP_CONTEXT.md` + `LONGSHOT_STATE_OF_PLAY.md`, commit.
5. Queued next hypotheses: evaluate arb-monitor scan data after ~1–2 weeks (frequency × depth vs
   costs); manifest-coverage de-confound is DONE; remaining config slots under the user's
   20-strategy authorization: 3.
